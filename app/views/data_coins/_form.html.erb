<%= form_for(@data_coin) do |f| %>
  <% if @data_coin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@data_coin.errors.count, "error") %> prohibited this data_coin from being saved:</h2>

      <ul>
      <% @data_coin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
